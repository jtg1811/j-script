// to check for empty array and empty object

const myarr = []
if (myarr.length === 0){
    console.log("array is empty")
}

const myobj = {}
if (Object.keys(myobj).length === 0){
    console.log("obj is empty")
}